# $EGGS public assets

## Basket artwork (required for production visuals)

Insert the transparent high-resolution basket PNG here:

```text
public/assets/eggs-basket.png
```

The interactive hotspots in `src/data/assets.ts` assume this artwork. Tune hotspot percentages after replacing the placeholder.

## Logos

Current files in `public/assets/logos/` are lightweight SVG placeholders. Replace with official or licensed logos when available.
